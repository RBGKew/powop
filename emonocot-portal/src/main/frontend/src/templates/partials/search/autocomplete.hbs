<div class="c-autocomplete {{theme}}">
  <ul class="c-autocomplete__list" tabindex="-1" role="listbox" aria-expanded="true">
  {{#each suggestions}}
    {{#each this}}
      <li role="option" class="c-autocomplete__option">
        <a href="#">{{ this }}</a>
        {{#if showSuggester}}
            <span class="in_line">{{getSuggester @../key}}</span>
        {{/if}}
      </li>
    {{/each}}
  {{/each}}
  </ul>
</div>
