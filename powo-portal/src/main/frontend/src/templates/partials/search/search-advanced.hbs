<div class="container">
  <div class="row"> 
    <form class="form-inline form-inline--advanced">
      {{>form-input}}
      {{>form-input}}
      {{>form-input}}
      {{>form-input}}
      {{>form-search}}
      {{>form-range}}
      {{>form-range}}
      {{>form-range}}
      {{>form-range}}
      {{>form-range}}
      {{>form-input}}
      {{>form-input}}
      {{>form-input}}
      {{>form-input}}
      {{>form-search}}
      {{>form-search}}
      {{>form-search}}
      {{>form-search}}
    </form>
  </div>
</div>